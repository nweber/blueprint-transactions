// Generated by CoffeeScript 1.10.0
var DELIMITER, ESCAPE_CHAR, escapeOriginPart, ref;

ref = require('./constants'), ESCAPE_CHAR = ref.ESCAPE_CHAR, DELIMITER = ref.DELIMITER;

escapeOriginPart = function(part) {
  var clonedPart;
  clonedPart = new String(part);
  return clonedPart.replace(new RegExp(DELIMITER, "g"), "" + ESCAPE_CHAR + DELIMITER);
};

module.exports = function(transaction) {
  var origin, path;
  origin = transaction['pathOrigin'];
  return path = escapeOriginPart(origin['apiName']) + DELIMITER + escapeOriginPart(origin['resourceGroupName']) + DELIMITER + escapeOriginPart(origin['resourceName']) + DELIMITER + escapeOriginPart(origin['actionName']) + DELIMITER + escapeOriginPart(origin['exampleName']);
};
